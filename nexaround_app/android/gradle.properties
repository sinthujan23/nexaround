org.gradle.jvmargs=-Xmx3072m -XX:MaxMetaspaceSize=1024m -XX:ReservedCodeCacheSize=512m -XX:+UseG1GC
android.useAndroidX=true
kotlin.incremental=false
MAPBOX_DOWNLOADS_TOKEN=sk.eyJ1IjoiaGFzaG5hdGUiLCJhIjoiY21vanFqZmhuMDFrNTJxc2IxdnNicTNtZyJ9.3nVHY2bg18Tc0qY9hwVaZQ
# Google Maps Android key. Leave BLANK to auto-fetch the admin-managed key from
# the backend /config/keys at build time (the DB stays the single source of truth).
# Set a real value here (or the GOOGLE_MAPS_API_KEY env var) only to override for
# local/offline builds.
GOOGLE_MAPS_API_KEY=
