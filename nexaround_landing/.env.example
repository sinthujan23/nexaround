# The landing chatbot (Neva) no longer uses a client-side API key.
# It calls the backend at /api/v1/landing/neva, which holds the Gemini key
# server-side. There are intentionally NO secrets in this frontend .env —
# any VITE_* value is compiled into the public bundle and must be non-secret.
